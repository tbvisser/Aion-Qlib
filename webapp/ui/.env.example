# Copy to webapp/ui/.env.local to override Vite dev defaults.
# See vite.config.ts for the API proxy settings.

# Show demo scheduled tasks in the Scheduled tab when no real tasks exist.
VITE_DEMO_SCHEDULED_TASKS=true
