[pytest]
testpaths = tests
asyncio_mode = auto
markers =
    unit: Unit tests (no external dependencies)
    api: API endpoint tests (requires running backend)
    slow: Slow tests (skip with -m "not slow")
    auth: Authentication and authorization tests
    isolation: Data isolation / RLS tests
